using System; //using (Keyword) system(namespace having classes in it)

namespace Helloworld //namespace (to organize the code) 
{
    class Program //class(have data and methods)
    {
        static void Main(string[] args) //Main(Program starts from here)
        {
            Console.WriteLine("Helloworld"); //Console(class of system namespace)  Writeline (to print output) / Write( to print output on same line)
            Console.ReadLine(); //Readline(to stay on console/ input from user)
            //Everyline of C# code ends with;
            //Case Sensitive 
            //Object oriented programming language
        }
    }
}
